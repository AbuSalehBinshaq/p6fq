# تشغيل ورفع بطل قصتي

## التشغيل محلياً

يتطلب المشروع Node.js 22 أو إصداراً أحدث وpnpm. بعد فك الضغط، شغّل:

```bash
pnpm install
pnpm check
pnpm test
pnpm build
pnpm start
```

## الرفع على استضافة Node.js

ارفع مجلد المشروع إلى الاستضافة، ثم عرّف متغيرات البيئة الخاصة بالمشروع، خصوصاً `DATABASE_URL` و`JWT_SECRET` ومتغيرات Manus الموجودة في إعدادات المشروع. استخدم أمر البناء `pnpm build` وأمر التشغيل `pnpm start`. لا تثبّت `node_modules` داخل ملف ZIP؛ الاستضافة يجب أن تثبّت الاعتمادات من `pnpm-lock.yaml`.

يجب أن تمرر الاستضافة المنفذ الذي توفره في متغير `PORT`، والمشروع لا يحتاج إلى منفذ ثابت. تأكد من تفعيل HTTPS، ومن عدم رفع أي ملف `.env` أو أسرار إلى المستودع.

## الملاحظات التشغيلية

الطلب الحالي يفتح تيليجرام إلى `@p6_fq`، والدفع يتم عبر رابط زيينة بعد اعتماد المعاينة. صور الأمثلة موجودة عبر روابط تخزين المشروع، لذلك يلزم أن تبقى هذه الروابط متاحة عند استخدام نسخة خارجية. صفحة Manus المنشورة متاحة أيضاً على:

https://batalstory-774btpap.manus.space

## تحقق إصدار ZIP

تم اختبار `batal-story-latest.zip` قبل التسليم، وتأكد وجود `package.json` و`pnpm-lock.yaml` و`server/_core/index.ts` و`client/index.html` و`DEPLOY.md` داخله. كما اجتاز الأرشيف فحص السلامة `unzip -tq` دون أخطاء.
