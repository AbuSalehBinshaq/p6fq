# روابط إحالة الأدمن

يُستخدم رمز إحالة ثابت ومميز لكل أدمن، ويُضاف إلى رابط الموقع بالمعامل `partner`. اختَر رمزاً واضحاً بلا مسافات، مثل `admin_ahmed` أو `group_dubai_1`.

## صيغة الرابط

```text
https://batal-story.onrender.com/?partner=admin_ahmed&utm_source=whatsapp&utm_medium=group&utm_campaign=wa_moms_aug
```

يمكن اختصار الرابط الطويل في Bitly بعد إنشائه. يجب استخدام اختصار مختلف لكل أدمن أو لكل قروب حتى تبقى النقرات وتقارير الحملات منفصلة.

## كيف يعمل الإسناد

عند فتح الرابط، يحفظ خادم الموقع رمز `partner` في كوكي موقعة لمدة 30 يوماً. الكوكي لا تحمل بيانات الطفل، ولا يرسل رمز الإحالة إلى Google Analytics أو Microsoft Clarity. عند إرسال النموذج، يقرأ الخادم الرمز الموقّع ويربطه بالطلب في قاعدة البيانات.

في لوحة `/orders` يظهر الطلب مع شارة `إحالة: admin_ahmed`، أو `طلب مباشر` إذا لم يدخل العميل من رابط أدمن. معامل `ref` محجوز للتوافق مع رقم الطلب القديم؛ استخدم `partner` في روابط الشركاء.

## مهم قبل احتساب العمولة

رمز الإحالة يثبت مصدر الطلب، لكنه لا يثبت وحده أن الطلب مدفوع أو مكتمل. راجع حالة الطلب في اللوحة قبل اعتماد أي عمولة، واتفق مع كل أدمن مسبقاً على نسبة العمولة ومدة الإسناد وسياسة الطلبات الملغاة.
